from setuptools import find_packages, setup

setup(
    mane = 'Info-retrieval-system',
    version = '0.0.1',
    author = 'Mukul Sharma',
    author_email = 'iammukul002@gmail.com',
    packages = find_packages(),
    install_requires = [],

)

